spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cepdb
    username: cepuser
    password: ceppass

aws:
  archive:
    enabled: true
    s3-bucket: cep-query-logs
  region: us-east-1
  access-key: test
  secret-key: test
  s3:
    endpoint: http://localhost:4566
